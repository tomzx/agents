# Open Questions

1. What is the ideal shared location for extracted checklist items (a `references/` file within the skill directory or a cross-skill shared reference)? (Resolved by constraint: must be in `skills/sdlc/references/`)

2. Should the shared reference be a new SKILL.md (with its own frontmatter and name) or a plain markdown file under `skills/sdlc/references/`?

3. Should the review checklist shared reference also include the evaluation detail templates (the `<details>` section content)?

4. Should the changelog entry (FR-07) point to a centralized changelog or be part of the skill file itself?
